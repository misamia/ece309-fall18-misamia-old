class ListNode{
public:
	List():
	// required functions
	push_back(string)		// add a string to the tail of a list
	get(n)					// return a copy of the n-th item in the list
	length()				// get the number of items in the list
	remove_front()			// remove the head of the list and return a copy of the data that was removed
	// constructor
	// destructor
}